<%= form_for([@member, @profile], url: upload_avatar_member_profile_path(@member), html: { multipart: true, method: "put" }) do |f| %>

  <%= f.hidden_field :member_id %>
  <%= f.file_field :avatar %>
  <%= f.submit "Upload", class: "btn btn-default btn-upload" %>
<% end %>